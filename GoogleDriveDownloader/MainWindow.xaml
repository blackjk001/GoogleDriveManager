<Window x:Class="GoogleDriveDownloader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GoogleDriveDownloader"
        mc:Ignorable="d"
        Title="WPF Drive Manager" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" FontSize="13"
        Background="#F7F9FC"
        x:Name="ThisWindow"
        SizeChanged="Window_SizeChanged"
        LocationChanged="Window_LocationChanged"
        Closing="MainWindow_Closing">

    <Window.Resources>
        <SolidColorBrush x:Key="GoogleBlue" Color="#1A73E8"/>
        <SolidColorBrush x:Key="GoogleBlueLight" Color="#E8F0FE"/>
        <SolidColorBrush x:Key="GoogleGrayBg" Color="#F8F9FA"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#3C4043"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#5F6368"/>
        <SolidColorBrush x:Key="BorderColor" Color="#DADCE0"/>

        <ContextMenu x:Key="FileSystemContextMenu" Background="White" BorderBrush="{StaticResource BorderColor}">
            <MenuItem x:Name="miOpen" Header="📂 Открыть" Click="Menu_Open_Click"/>
            <Separator Background="{StaticResource BorderColor}"/>
            <MenuItem x:Name="miCopy" Header="📄 Копировать" Click="Menu_Copy_Click"/>
            <MenuItem x:Name="miCut" Header="✂️ Вырезать" Click="Menu_Cut_Click"/>
            <MenuItem x:Name="miPaste" Header="📋 Вставить" Click="Menu_Paste_Click"/>
            <Separator Background="{StaticResource BorderColor}"/>
            <MenuItem x:Name="miRename" Header="✏️ Переименовать" Click="Menu_Rename_Click"/>
            <MenuItem x:Name="miDelete" Header="🗑️ Удалить" Click="Menu_Delete_Click" Foreground="#D93025"/>
        </ContextMenu>

        <Style TargetType="Button" x:Key="MaterialButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#15000000"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#25000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButton" BasedOn="{StaticResource MaterialButton}">
            <Setter Property="Background" Value="{StaticResource GoogleBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="18" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="1" BlurRadius="4" Opacity="0.2"/>
                            </Border.Effect>

                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#185ABC"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="2" BlurRadius="6" Opacity="0.3"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>        

        <Style TargetType="TextBox" x:Key="SearchTextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#F1F3F4"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="20" Background="{TemplateBinding Background}" BorderThickness="0">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="10,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="Background" Value="White"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="1" BlurRadius="4" Opacity="0.2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FileItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="ContextMenu" Value="{StaticResource FileSystemContextMenu}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="8,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="bd" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4"
                                Margin="0,1">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#F1F3F4"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource GoogleBlueLight}"/>
                                <Setter Property="Foreground" Value="{StaticResource GoogleBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="FileDataTemplate">
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="370"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>

                <TextBlock  x:Name="Icon" Text="📄" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#5F6368"/>

                <TextBlock Grid.Column="1" 
                   Text="{Binding Name}" 
                   VerticalAlignment="Center" 
                   Margin="0,0,10,0" 
                   TextTrimming="CharacterEllipsis" 
                   ToolTip="{Binding Name}"/>

                <TextBlock Grid.Column="3" 
                   Text="{Binding FormattedSize}" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Right" 
                   Margin="0,0,10,0"
                   Foreground="#5F6368"/>
            </Grid>

            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding IsFolder}" Value="True">
                    <Setter TargetName="Icon" Property="Text" Value="📁"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Name}" Value="C:/">
                    <Setter TargetName="Icon" Property="Text" Value="🖴"/>
                    <Setter TargetName="Icon" Property="Foreground" Value="#1A73E8"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>

    </Window.Resources>

    <Grid x:Name="MainGrid" Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="15"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="16" Padding="15">
            

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Этот компьютер" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" Margin="5,0,0,15"/>

                <Grid Grid.Row="1" Margin="0,8,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="btnWindowsUp" Content="🡅" Width="40" Height="36" 
                            Style="{StaticResource MaterialButton}" FontSize="18"
                            Click="BtnWindowsUp_Click" ToolTip="На уровень вверх"/>

                    <TextBox x:Name="txtWindowsCurrentPath" Grid.Column="1" Margin="10,0,0,0" 
                             Style="{StaticResource SearchTextBox}"
                             IsReadOnly="True" VerticalContentAlignment="Center"/>
                </Grid>

                <Border Grid.Row="2" Background="#F1F3F4" Margin="0,5,0,0" Padding="0,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="370"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="1" Text="Имя" FontWeight="SemiBold" Foreground="#5F6368" Cursor="Hand"
                       MouseLeftButtonUp="Windows_SortName_Click" Margin="5,0,0,0"/>

                        <TextBlock Grid.Column="2" Text="Размер" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="#5F6368" Cursor="Hand"
                       MouseLeftButtonUp="Windows_SortSize_Click" Margin="0,0,10,0"/>
                    </Grid>
                </Border>


                <ListBox x:Name="lstWindowsFiles" Grid.Row="3"
                         
                         ContextMenu="{StaticResource FileSystemContextMenu}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         HorizontalContentAlignment="Stretch"
                         BorderThickness="0" Background="Transparent"
                         MouseDoubleClick="Universal_MouseDoubleClick"
                         SelectionMode="Extended"
                         PreviewMouseLeftButtonDown="Universal_PreviewMouseLeftButtonDown"
                         PreviewMouseMove="Universal_PreviewMouseMove"
                         PreviewMouseLeftButtonUp="Universal_PreviewMouseLeftButtonUp"
                         ContextMenuOpening="Universal_ContextMenuOpening"
                         ItemContainerStyle="{StaticResource FileItemStyle}"
                         ItemTemplate="{StaticResource FileDataTemplate}"
                         AllowDrop="True"
                         DragEnter="LstWindowsFiles_DragEnter"
                         Drop="LstWindowsFiles_Drop"/>
            </Grid>
        </Border>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" 
                      Background="Transparent" ShowsPreview="True"/>

        <Border Grid.Column="2" Background="White" CornerRadius="16" Padding="15">
           
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ListBox x:Name="lstAccounts" 
                             SelectionChanged="LstAccounts_SelectionChanged"
                             Background="Transparent" BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                             ScrollViewer.VerticalScrollBarVisibility="Disabled">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border x:Name="BorderContainer" CornerRadius="16" Background="#F1F3F4" Padding="12,6" Margin="0,0,8,0">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Email}" VerticalAlignment="Center" Foreground="{StaticResource TextPrimary}" FontWeight="Medium"/>
                                        <Button Content="✕" Click="BtnRemoveAccount_Click" Margin="8,0,0,0"
                                                Width="16" Height="16" Background="#9AA0A6" Foreground="White"
                                                FontSize="10" FontWeight="Bold" Padding="0" BorderThickness="0">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="8"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </StackPanel>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                        <Setter TargetName="BorderContainer" Property="Background" Value="{StaticResource GoogleBlueLight}"/>
                                        <Setter TargetName="BorderContainer" Property="TextBlock.Foreground" Value="{StaticResource GoogleBlue}"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>

                    <Button x:Name="btnAddAccount" Grid.Column="1" 
                            Content="+ Добавить" 
                            Click="BtnAddAccount_Click"
                            Style="{StaticResource PrimaryButton}"
                            FontSize="12" Height="32"/>
                </Grid>

                <Grid Grid.Row="1" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="btnGoogleDriveUp" Content="🡅" Width="40" Height="36" 
                            Style="{StaticResource MaterialButton}" FontSize="18"
                            Click="BtnGoogleDriveUp_Click" ToolTip="На уровень вверх"/>

                    <TextBox x:Name="txtGoogleDriveCurrentPath" Grid.Column="1" Margin="10,0,0,0" 
                             Style="{StaticResource SearchTextBox}"
                             IsReadOnly="True" VerticalContentAlignment="Center"/>
                </Grid>

                

                <Border Grid.Row="2" Background="#F1F3F4" Margin="15,5,15,0" Padding="0,8">
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="370"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="1" Text="Имя" FontWeight="SemiBold" Foreground="#5F6368" Cursor="Hand"
                       MouseLeftButtonUp="Google_SortName_Click" Margin="5,0,0,0"/>

                        <TextBlock Grid.Column="2" Text="Размер" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="#5F6368" Cursor="Hand"
                       MouseLeftButtonUp="Google_SortSize_Click" Margin="0,0,10,0"/>
                    </Grid>
                </Border>



                <ListBox x:Name="lstGoogleDriveFiles" 
                         Margin="0, 0, 0, 0"
                         Grid.Row="3" 
                         ContextMenu="{StaticResource FileSystemContextMenu}"
                         BorderThickness="0" Background="Transparent"
                         MouseDoubleClick="Universal_MouseDoubleClick"
                         SelectionMode="Extended"
                         PreviewMouseLeftButtonDown="Universal_PreviewMouseLeftButtonDown"
                         PreviewMouseMove="Universal_PreviewMouseMove"
                         PreviewMouseLeftButtonUp="Universal_PreviewMouseLeftButtonUp"
                         ContextMenuOpening="Universal_ContextMenuOpening"
                         ItemContainerStyle="{StaticResource FileItemStyle}"
                         ItemTemplate="{StaticResource FileDataTemplate}"/>
            </Grid>
        </Border>

        <Grid x:Name="PopupAnchor" Grid.Column="0" Grid.ColumnSpan="3" VerticalAlignment="Bottom" HorizontalAlignment="Left" Width="320" Height="1" Margin="25,0,0,25" Background="Transparent"/>

        <Popup x:Name="DownloadPopup" Placement="Top" PlacementTarget="{Binding ElementName=PopupAnchor}" AllowsTransparency="True" StaysOpen="True">
            <Border Background="White" CornerRadius="8" Width="320">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="4" BlurRadius="15" Opacity="0.2"/>
                </Border.Effect>
                <StackPanel>
                    <Grid Background="#333333" Height="40">
                        <TextBlock Text="Загрузки" VerticalAlignment="Center" Margin="15,0,0,0" FontWeight="SemiBold" Foreground="White"/>
                        <Button x:Name="btnTogglePopup" Content="▼" Click="BtnTogglePopup_Click" HorizontalAlignment="Right" Width="40" Background="Transparent" Foreground="White" BorderThickness="0"/>
                    </Grid>
                    <ListBox x:Name="lstDownloads" Background="White" BorderThickness="0" ItemsSource="{Binding ElementName=ThisWindow, Path=ActiveDownloads}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="10,8" Width="290">
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Background="#F1F3F4" CornerRadius="4" Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="📄" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" Margin="10,0">
                                        <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" FontSize="12"/>
                                        <TextBlock Text="{Binding Status}" FontSize="10" Foreground="Gray"/>
                                    </StackPanel>

                                    <Button Grid.Column="2" Content="✕" Command="{Binding CancelCommand}" Style="{StaticResource MaterialButton}" Width="20" Height="20" FontSize="10" Padding="0"/>

                                    <ProgressBar Grid.Row="1" Grid.ColumnSpan="3" Value="{Binding Progress}" Height="2" Margin="0,5,0,0" Background="#F1F3F4" Foreground="{StaticResource GoogleBlue}" BorderThickness="0"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>
        </Popup>

        <Popup x:Name="RenamePopup" Placement="Center" PlacementTarget="{Binding ElementName=MainGrid}" StaysOpen="False" AllowsTransparency="True">
            <Border Background="White" CornerRadius="8" Padding="20" Width="300">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" Opacity="0.3" ShadowDepth="5"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Переименовать" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,15"/>
                    <TextBox x:Name="txtRenameInput" Style="{StaticResource SearchTextBox}" Margin="0,0,0,20"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Отмена" Click="BtnRenameCancel_Click" Style="{StaticResource MaterialButton}" Foreground="{StaticResource GoogleBlue}" Margin="0,0,10,0"/>
                        <Button Content="ОК" Click="BtnRenameOk_Click" Style="{StaticResource PrimaryButton}" Padding="20,6"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Popup>

    </Grid>
</Window>